# 1. Testing polymarket

It is simple, just check the test-p.json

# 2. Testing kalshi

Keep your key file at the scripts folder in the project root (not the cre one). Then run the `kalshi:update-test` file

It will update the headers in test-k

Then update the other values like ticker and all and run cre simulate
